import { mergeStyles } from "@fluentui/react";

export const hireContainerStyles = mergeStyles({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export const cardStyles = mergeStyles({
  width: "49%",
});
