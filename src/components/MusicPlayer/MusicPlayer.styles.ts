import { mergeStyles } from "@fluentui/react";

export const musicContainerStyles = mergeStyles({
  margin: "80px 0",
  textAlign: "center",
});

export const playerStyles = mergeStyles({
  display: "flex",
  justifyContent: "center",
});
